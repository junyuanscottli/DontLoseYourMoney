---
title: <中文标题>
type: fact
author: junyuan
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
status: draft
expires:
confidence: medium
links: []
---

# <中文标题>

正文。

`type: fact` 只写可验证的事实，判断和推演另开 `type: analysis` 的笔记。
有时效性的机会必须填 `expires`；过期后把 `status` 改成 `stale`，不要删除。
