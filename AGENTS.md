# AGENTS.md

本文件是这个仓库的**唯一协议层**。任何 agent 在本仓库执行读写前，先读完本文件。

---

## 1. 这个仓库是什么

两个人共同运作的**研究文字库**。沉淀对赚钱机会的研究：事实、分析、结论、以及彼此发布与认领的任务。

- **不存代码。** 这里是文字库。
- **没有独立的 wiki 层。** 文字库本身就是事实层。
- **没有独立的任务看板。** 任务就地发布在它所属的研究路线里。走进一条路线，就能看到这条路线的全部现状与待办。
- **不预设分类。** 不区分「套利」和「价投」。只要是能赚钱的机会，就起一条研究路线，具体路线具体分析。

## 2. 结构

```
AGENTS.md          本文件，协议
README.md          入口，活跃路线一览
research/
  <route-slug>/
    README.md      路线门面：现状 / 开放问题 / 任务
    <note>.md      事实与洞见的沉淀
templates/         新建路线与笔记时复制的模板
```

平铺。`research/` 下不做二级分类目录。若日后多条路线反复引用同一批基础知识（税务、法律、会计等），再抽象出 `foundations/`；**在那之前不要预先创建**。

## 3. 研究路线

### 新开一条路线

1. `research/<route-slug>/` —— slug 用英文 kebab-case（跨平台与工具友好），正文用中文
2. 复制 `templates/route-README.md` 为该目录的 `README.md`，填好 frontmatter
3. 在根 `README.md` 的路线表中**追加一行**

第 3 步是硬性要求。根 README 是这个平铺结构唯一的导航面，漏登记等于路线不存在。

### 路线 README 的 frontmatter

```yaml
---
route: credit-card-churning     # 与目录名一致
title: 信用卡开户奖励            # 中文名
status: active                  # active | paused | closed
opened: 2026-09-11
tags: []                        # 自由打标，用来横向串联，不是目录分类
---
```

`tags` 是可选的横向线索（比如 `开户奖励` `时效性强` `需报税`）。想按主题聚合时靠标签，不靠目录。

### 路线 README 的固定小节

`## 现状` · `## 开放问题` · `## 任务`

`现状` 写这条路线当前的判断和已验证的结论摘要（细节进笔记文件）。`开放问题` 写还没想清楚、但也还没具体到能派任务的东西。

## 4. 任务

任务写在所属路线 README 的 `## 任务` 小节。**格式锁死**，agent 用正则解析，人直接可读：

```
- [状态] `T-NNN` 描述 — 认领: @who — 开: YYYY-MM-DD
```

| 状态 | 含义 |
|---|---|
| `[ ]` | 待认领 |
| `[~]` | 进行中 |
| `[x]` | 已完成 |
| `[-]` | 已放弃 |

规则：

- **任务 ID 按路线各自编号**（每条路线都从 `T-001` 开始），不做全局编号
- 未认领时 `认领:` 后写 `—`
- 完成时把 `— 开: <date>` 替换为 `— 完成: <date> → <产出文件>`，指向沉淀下来的笔记
- 放弃时在行末补 `（原因：…）`
- **不要删除已完成或已放弃的任务行**，它们是这条路线的决策记录

示例：

```markdown
## 任务
- [ ] `T-003` 验证 5/24 规则对 business card 的适用性 — 认领: — — 开: 2026-09-11
- [~] `T-002` 奥斯汀三个区 concession 实盘询价 — 认领: @xiping — 开: 2026-09-05
- [x] `T-001` 期权 basis 数据源对比 — 完成: 2026-09-08 → `data-sources.md`
- [-] `T-000` 跟踪某已下架活动 — 开: 2026-08-20（原因：活动已终止）
```

## 5. 笔记

文件名英文 kebab-case，正文中文。frontmatter：

```yaml
---
title: 中文标题
type: fact          # fact 可验证事实 | analysis 分析判断 | playbook 可执行步骤 | log 过程记录
author: junyuan     # junyuan | xiping
created: 2026-09-11
updated: 2026-09-11
status: draft       # draft 初稿 | verified 已验证 | stale 已失效
expires:            # 机会有时效性时必填（活动截止、政策生效日等）；无时效留空
confidence: medium  # high | medium | low
links: []           # 相关笔记的相对路径
---
```

关于几个字段：

- **`type: fact` 与 `analysis` 要分开。** 事实可被对方直接引用，判断需要对方自己过一遍脑子。混在一起会让另一个人误把观点当依据。
- **`expires` 是这个库的生命线。** 开户奖励会下架、优惠会过期、政策会改。过期未更新的笔记应被改成 `status: stale`，而不是删掉——失效本身是有价值的信息。
- **`confidence: low` 不是缺点**，标出来比不写强。

## 6. 与个人研究库的分界

两人各自维护独立的个人研究库。分界线：

| | 个人研究库 | 本仓库 |
|---|---|---|
| 装什么 | 过程、原始材料、半成品、私人推演 | 结论、可复用的事实、任务交换 |
| 给谁看 | 自己 | 对方 |

判断标准：**这条东西对方需要吗？**需要就升上来，不需要就留在自己库里。升上来时要重写成对方能直接用的形态，而不是原样粘贴。

## 7. 红线

即使本仓库是 private，以下内容**永不提交**：

- 卡号、账号、密码、API key、SSN、驾照号
- 具体账户余额、具体持仓金额
- 他人（非本仓库两位成员）的个人身份信息
- 任何违反平台条款或法律的具体操作指引

收益相关用比例、区间或量级表述，不写精确金额。

## 8. Agent 行为约定

- **两人并行写作。** 动手前先 `git pull`；不要改写对方笔记的结论，有异议就新开一个 `type: analysis` 的笔记并 `links` 过去，或在对方路线里发一条任务
- **不删除他人的文件和任务行。** 失效就标 `stale` / `[-]`，保留痕迹
- **改动笔记必须同步 `updated` 字段**
- **新开路线必须登记根 README**（见 3.1）
- commit message 用中文，格式 `<路线 slug>: 做了什么`，跨路线或协议改动用 `meta: …`
- 涉及推送到远端、邀请协作者、修改仓库设置等对外动作，先跟人确认
